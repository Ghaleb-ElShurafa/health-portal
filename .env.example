ANTHROPIC_API_KEY=your_key_here

# Optional: only needed for a hosted deployment where local disk storage
# isn't reliably persistent. Leave unset to keep using local SQLite.
TURSO_DATABASE_URL=libsql://your-db-name.turso.io
TURSO_AUTH_TOKEN=your_turso_auth_token
